services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_USER: timeturtle
      POSTGRES_PASSWORD: dev-only-password
      POSTGRES_DB: timeturtle
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql
volumes:
  postgres-data:
