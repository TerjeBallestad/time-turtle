node_modules/
client/dist/
server/data/
.DS_Store
*.log

# pm system lives in its own nested git repo (SB-015) — kept locally, untracked here
.pm/
pm.config.json

# agent skills and commands ARE tracked — they document how this project is worked,
# and untracked meant an agent could silently degrade a protocol with no diff to revert.
# Local/machine state stays out.
.claude/*
!.claude/skills/
!.claude/commands/
.claude/worktrees/
.claude/settings.local.json

# stripped from public tree per DD-005 (kept locally, untracked)
design-import/
